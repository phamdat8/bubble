<nav class="navbar navbar-expand-lg navbar-light bg-pink fixed-top">
    <div class="container-fluid">
        <a href="/"><div class="name-text">Bubble</div></a>
        <%if current_user.present? %>
            <div>
                <div class="dropdown">
                    <div class="dropdown-toggle" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                        <% if current_user.image.present? %>
                            <%= image_tag current_user.image, class: "img-nav" if current_user.image.present?%>
                        <% else %>
                            <img src="<%= image_url('bubble1.jpg') %>" class="img-nav">
                        <% end %>
                    </div>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
                        <li><a class="dropdown-item" href="/">Trang chính</a></li>
                        <li><a class="dropdown-item" href="/user/posts">Cá nhân</a></li>
                        <li><a class="dropdown-item" data-method="delete" href="/users/sign_out">Đăng xuất</a></li>
                    </ul>
                </div>
            </div>
        <% else %>
            <div class="d-flex">
                <div class="button_container m-2">
                    <a href="/users/sign_in"><button class="btn-layout"><span>Đăng nhập</span></button></a>
                </div>
                <div class="button_container m-2">
                    <a href="/users/sign_up"><button class="btn-layout"><span>Đăng ký</span></button></a>
                </div>
            </div>
        <% end %>
    </div>
</nav>
